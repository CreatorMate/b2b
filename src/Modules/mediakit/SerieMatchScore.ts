export type SerieMatchScore = {
    id: number,
    serie_id: number,
    brand_id: number,
    match_score: number,
    average_per_post: string,
    new_vs_existing: string,
    followers_interested: string,
    impressions: string
}